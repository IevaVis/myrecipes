<%= render 'shared/page_title', title: "Chef #{@chef.chefname}'s profile" %>

<div class="row">
	<div class="col-md-4 offset-md-4 center">
		<%= gravatar_for @chef, size: 150 %>
	</div>
</div>

<% if @chef.recipes.exists? %>
<h4 align="center"><%= @chef.chefname %>'s recipes</h4>
<%= will_paginate @chef_recipes, list_classes: %w(pagination justify-content-center) %>
<%= render 'recipes/recipe', obj: @chef_recipes %>
<%= will_paginate @chef_recipes, list_classes: %w(pagination justify-content-center) %>
<% else %>
<h4 align="center"><%= @chef.chefname %> has no recipes</h4>
<% end %>

